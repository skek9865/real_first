이 프로젝트는 대학교 실험실 게시판/커뮤니티 기능을 하는 웹 사이트를 제작한 프로젝트 이다.

rest api 방식을 사용해 직접 api를 만듦으로써 프론트개발을 한 팀원이 서버를 올려서 내 api 를 불러오면 DB에 정보를 가져오게 설계를 했다.

Controller 에서 모든 url 요청을 매핑하게 만들었고 Service 에서 모든 비즈니스 로직을 처리하게 만들었다.
데이터 베이스는 maria 를 썻고 Spring Data Jpa 를 활용해 DB와 통신을 했고 그 사이에는 Entity, Dto 를 만들어 DB는 Entity가 client 쪽은 Dto 형식으로 처리하게 설계하였다.
